Service/data seeds live here. Suggested files:

- `services_seed.json`: categories and services to preload.
- `users_seed.json` (future): demo users.
- `domain_seed.json` (future): vehicles, workers, violations, etc.
